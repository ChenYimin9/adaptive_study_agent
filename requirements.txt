# Core Web framework
streamlit>=1.35.0  # Used for building interactive Web interfaces

# Database connection
mysql-connector-python>=8.0.33  # Connecting to the MySQL Database

# Data processing and analysis
pandas>=2.0.3  # Data processing and analysis
numpy>=1.26.0  # Numerical computation (pandas dependency)

# Data visualization
plotly>=5.20.0  # Interactive chart generation
plotly-express>=0.4.1  # Simplified plotly interface

# LLM and API calls
openai>=1.30.0  # Used to invoke models such as DeepSeek that are compatible with OpenAI apis

# PDF generation
reportlab>=4.0.8  # Generate PDF certificates and reports
PyPDF2 >=3.0.1  # Process PDF files
python-docx >=1.1.2  # Generate a Word document

# Environmental configuration
python-dotenv>=1.0.0  # Manage environment variables (such as API keys, database passwords)

# Time processing
python-dateutil>=2.8.2  # Extended Date and Time processing (pandas dependency)
pytz>=2024.1  # Time zone processing

streamlit-autorefresh